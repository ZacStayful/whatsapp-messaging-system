/**
 * Placeholder route.
 *
 * Deliberately empty of behaviour: Phase 0a delivers the environment layer and
 * nothing else. This exists only so the project builds and the App Router has a
 * root route.
 */
export default function Page() {
  return (
    <main>
      <h1>Stayful WhatsApp</h1>
      <p>Phase 0a — environment layer only. No application code yet.</p>
    </main>
  );
}
